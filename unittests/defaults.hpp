#pragma once

#include <chrono>

static constexpr std::chrono::milliseconds REGWAIT_MSEC = std::chrono::milliseconds(100);
static constexpr std::chrono::seconds REGDEBUG_MSEC = std::chrono::seconds(100);
